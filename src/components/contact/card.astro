---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="bg-off border border-[#e2e8f0] p-5 duration-300">
  <div class="text-[0.58rem] tracking-[0.28em] uppercase text-blue2 mb-2 font-syne">
    {title}
  </div>

  <slot />
</div>
