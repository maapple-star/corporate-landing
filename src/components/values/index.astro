---
import ValueCard from "./value-card.astro";
import { values } from "./values-data";
---

<!-- VALUES -->
<div
  class="px-[6%] mb-[100px] grid grid-cols-4 gap-6 max-[900px]:grid-cols-2 max-[900px]:grid-cols-1"
>
  {values.map((value) => <ValueCard {...value} />)}
</div>
