---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a
  href={href}
  class={`text-[0.85rem] text-[#64748b] hover:text-white no-underline transition-colors duration-200 ${className}`}
>
  <slot />
</a>
