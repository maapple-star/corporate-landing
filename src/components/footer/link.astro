---
interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a
  href={href}
  class={`text-[0.85rem] text-white/80 hover:text-white no-underline transition-colors duration-200 ${className}`}
>
  <slot />
</a>
